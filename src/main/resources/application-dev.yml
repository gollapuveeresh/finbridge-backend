# Local development overrides — activate with: SPRING_PROFILES_ACTIVE=dev
# Never use this profile in production (verbose logging can leak PII; SQL echo is slow).
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.finbridge: DEBUG
    org.hibernate.SQL: DEBUG
